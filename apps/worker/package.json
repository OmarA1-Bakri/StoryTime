{
  "name": "@storytime/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:e2e": "echo \"No worker e2e tests yet\"",
    "build": "tsc --noEmit"
  },
  "dependencies": {
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
