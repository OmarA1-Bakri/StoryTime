# Local MinIO

StoryTime uses S3-compatible object storage locally through MinIO.

Default local values:

```text
LOCAL_S3_ENDPOINT=http://localhost:9000
LOCAL_S3_ACCESS_KEY=minioadmin
LOCAL_S3_SECRET_KEY=minioadmin
LOCAL_S3_BUCKET=storytime-dev
```

Create the local bucket before running media tests.
