# Convex

StoryTime Convex backend schema and function workspace.

Current implementation status:

- PRD v0.5 schema work is being added in small vertical slices.
- Mutations/actions are added behind authorization gates.
- Live sessions, replay records, usage records, and audit records are represented in backend modules.

Run locally once Convex project linkage is configured:

```bash
pnpm convex:dev
```
